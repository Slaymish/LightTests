// Shader.cpp
#include "Shader.hpp"

void Shader::bind() {
  // Bind the shader for rendering
}

void Shader::unbind() {
  // Unbind the shader after rendering
}

void Shader::setUniform(const std::string &name, float value) {
  // Set uniform variable in the shader
}
