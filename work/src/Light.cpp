// Light.cpp
#include "Light.hpp"
#include <glm/glm.hpp>

void Light::setPosition(const glm::vec3 &pos) {
  // Set the light position in the scene
}

void Light::setColor(const glm::vec3 &color) {
  // Set the light color in the scene
}
